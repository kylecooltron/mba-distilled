
const Footer = ( { logo } ) => {
  return (
    <div>
      <p style={{textAlign: "center"}}>(Footer: contact info, social media links, etc.)</p>
      <br></br>
      <img src={logo} alt="logo" />
    </div>
  )
}

export default Footer